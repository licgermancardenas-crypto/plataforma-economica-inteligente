# Plataforma Económica Inteligente — dependencias
# Etapa actual: ingesta y análisis. Se irá ampliando por etapas.
#
# Cotas superiores a propósito: el deploy resuelve estas versiones en cada build,
# así que un major nuevo (pandas 3, plotly 6, streamlit 1.61 — todos aparecieron
# después de escribir este código) entra sin aviso y puede romper la app en el
# server mientras en local sigue andando. Probado contra pandas 2.2/3.0,
# streamlit 1.40/1.61 y plotly 5/6.

# --- Ingesta / datos ---
requests>=2.32,<3
pandas>=2.2,<4
numpy>=2.1,<3

# --- Análisis estadístico / econometría (etapas 6-7) ---
statsmodels>=0.14,<0.16        # OLS, VAR, VECM, ADF/KPSS, X-13 wrapper
scipy>=1.13,<2

# --- Dashboard (etapa 4-5) ---
streamlit>=1.37,<2
plotly>=5.22,<7

# --- ML / nowcasting (etapa 7) ---
scikit-learn>=1.5,<2        # ElasticNet para nowcasting de inflación

# --- IA (etapa 8) ---
# cliente del proveedor de LLM que se elija

# --- Tests ---
pytest>=8.0,<9
